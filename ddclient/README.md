# Dependencies
- Linux system with apt
- systemd
- gpg
- sed

# Usage

    make help
    sudo make install

# What it does
- Installs ddclient.
- Installs ddclient configuration.
- Enabled and starts the ddclient systemd unit.
